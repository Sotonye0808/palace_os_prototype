-- Add scheduled_time column to orders table for meal scheduling (pre-order for future time)\n\nALTER TABLE public.orders \nADD COLUMN IF NOT EXISTS scheduled_time TIMESTAMP WITH TIME ZONE;\n\n-- Add index for scheduled_time queries\nCREATE INDEX IF NOT EXISTS idx_orders_scheduled_time ON public.orders(scheduled_time);\n\n-- Update policies to allow viewing orders by scheduled time\n-- (Existing policies already cover this as they're based on user_id)
